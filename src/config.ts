export const DEFAULT_FALLBACK_MODEL = "gpt-5-codex";
export const DEFAULT_PRICING_MODE = "api";
export const DEFAULT_LOCALE = "en-US";
export const PRICING_SNAPSHOT_VERSION = "openai-pricing-2026-03-23";

export const SUPPORTED_COMMANDS = new Set([
  "daily",
  "monthly",
  "session",
  "summary",
  "inspect-events",
]);
